package com.intellias.basicsandbox.controller.validation.currency;

/**
 * Enum is used for currency code validation.
 * You can extend it by adding new codes in accordance with ISO 4217.
 */
public enum CurrencyCode {
    USD,
    EUR,
    UAH
}